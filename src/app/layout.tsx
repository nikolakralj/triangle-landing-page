import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-sans",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Electrical Installation Crews for Industrial, Rail & Data-Center Projects | Triangle Services",
  description: "Supervised electrical crews of 4–20+ for industrial, rail, data-center and HVAC projects across Austria, Germany and CEE. 10–14 day mobilisation. A1 and posting-ready.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${inter.variable} h-full antialiased scroll-smooth`}
    >
      <body className="min-h-full flex flex-col font-sans">{children}</body>
    </html>
  );
}

